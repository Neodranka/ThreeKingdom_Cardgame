# 삼국지 카드 게임 프로젝트 보고서

---

## 표지

# **삼국지 카드 게임**
## (Three Kingdoms Card Game)

### 전공심화 프로젝트

---

| 역할 | 이름 | 학번 |
|------|------|------|
| 팀장 | [이름 입력] | [학번 입력] |
| 팀원 | [이름 입력] | [학번 입력] |
| 팀원 | [이름 입력] | [학번 입력] |
| 팀원 | [이름 입력] | [학번 입력] |

**제출일:** 2025년 12월

**지도교수:** [교수명 입력]

---

## 요약문

### 프로젝트 개요

본 프로젝트는 중국 고전 소설 '삼국지연의'를 배경으로 한 **턴제 카드 게임**을 Unity 엔진으로 개발한 것이다. 플레이어는 유비, 관우, 조조, 주유 등 삼국지의 유명 장수를 선택하여 전략적인 카드 대전을 즐길 수 있다.

### 주요 기능

1. **스토리 모드**: 3개 전역(토동지전, 관도대전, 적벽대전), 12개 전투 스테이지
2. **대전 모드**: 신분장 시스템(주군, 충신, 반적, 내간)
3. **AI 시스템**: 4단계 난이도(랜덤, 쉬움, 보통, 어려움)
4. **스킬 시스템**: 패시브, 액티브, 트리거 등 20개 이상의 스킬
5. **다국어 지원**: 중국어, 영어, 한국어 지원

### 기술 스택

- **게임 엔진**: Unity 3D
- **프로그래밍 언어**: C# (22,090줄)
- **UI 프레임워크**: UGUI, TextMeshPro
- **디자인 패턴**: Singleton, Factory, Observer, Strategy, State

### 개발 성과

- 48개 C# 스크립트 파일
- 162개 리소스 파일 (306MB)
- 30명 이상의 무장 캐릭터
- 79장의 카드 (기본패, 계략패, 장비패)

### 결론

본 프로젝트를 통해 게임 개발의 전체 생명주기를 경험하였으며, 특히 AI 설계, 이벤트 시스템, 모듈화 아키텍처 등 전문적인 소프트웨어 공학 역량을 함양하였다.

---

**YouTube 시연 영상:** [https://youtube.com/watch?v=XXXXX](여기에_유튜브_URL_입력)

---

## 1. 개요

### 1.1 프로젝트 소개

**삼국지 카드 게임**은 중국 고전 소설 '삼국지연의'의 세계관을 배경으로 한 싱글플레이어 턴제 카드 게임이다. 플레이어는 다양한 삼국지 장수 중 하나를 선택하여 전략적인 카드 대전을 즐길 수 있다.

### 1.2 게임 장르

| 항목 | 내용 |
|------|------|
| 장르 | 턴제 카드 게임 (TCG/CCG) |
| 플랫폼 | PC (Windows) |
| 엔진 | Unity 3D |
| 개발 언어 | C# |

### 1.3 프로젝트 규모

```
총 코드 라인: 22,090줄
스크립트 파일: 48개
게임 씬: 4개
리소스 파일: 162개
리소스 용량: 306MB
```

---

## 2. 프로젝트 동기

### 2.1 개발 배경

1. **삼국지 IP의 인기**: 삼국지는 동아시아에서 가장 인기 있는 역사 소설 중 하나로, 풍부한 캐릭터와 스토리를 제공한다.

2. **카드 게임의 전략성**: 하스스톤, 마비노기 듀얼 등 디지털 카드 게임의 성공 사례를 참고하여, 전략적 깊이와 접근성을 동시에 갖춘 게임을 목표로 했다.

3. **Unity 학습 목적**: 게임 개발의 전체 과정(기획, 설계, 구현, 테스트)을 경험하고, Unity 엔진과 C# 프로그래밍 역량을 향상시키고자 했다.

### 2.2 목표

- 완성도 높은 싱글플레이어 카드 게임 개발
- AI 시스템을 통한 다양한 난이도 제공
- 스토리 모드를 통한 몰입감 있는 게임 경험
- 모듈화된 아키텍처로 확장 가능한 시스템 구축

---

## 3. 핵심 시나리오

### 3.1 게임 플로우

```
┌─────────────┐
│   메인메뉴   │
└──────┬──────┘
       │
   ┌───┴───┐
   │       │
   ▼       ▼
┌──────┐ ┌──────────┐
│ 대전 │ │ 스토리   │
│ 모드 │ │ 모드     │
└──┬───┘ └────┬─────┘
   │          │
   ▼          ▼
┌──────┐ ┌──────────┐
│ 무장 │ │ 전역 선택 │
│ 선택 │ │          │
└──┬───┘ └────┬─────┘
   │          │
   ▼          ▼
┌──────────────────┐
│     전투 씬      │
│  (턴제 카드대전)  │
└────────┬─────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌──────┐  ┌──────┐
│ 승리 │  │ 패배 │
└──────┘  └──────┘
```

### 3.2 스토리 모드 전역

#### 전역 1: 토동지전 (討董之戰)

| 스테이지 | 전투명 | 주요 캐릭터 | 승리 조건 |
|----------|--------|-------------|-----------|
| 1 | 회맹 | 조조 | 모든 적 격파 |
| 2 | 온주참화웅 | 관우 | 화웅 격파 |
| 3 | 삼영전여포 | 장비, 관우, 유비 | 여포 격파 |
| 4 | 토동 | 조조 | 동탁 격파 |

#### 전역 2: 관도대전 (官渡之戰)

| 스테이지 | 전투명 | 주요 캐릭터 | 승리 조건 |
|----------|--------|-------------|-----------|
| 1 | 관우참안량 | 관우 | 안량 격파 |
| 2 | 관우참문추 | 관우 | 문추 격파 |
| 3 | 오소습격 | 조조 | 모든 적 격파 |
| 4 | 관도결전 | 조조 | 원소 격파 |

#### 전역 3: 적벽대전 (赤壁之戰)

| 스테이지 | 전투명 | 주요 캐릭터 | 특수 규칙 |
|----------|--------|-------------|-----------|
| 1 | 장판선봉 | 조운 | 용담 튜토리얼 |
| 2 | 장비단교 | 장비 | 담렬 보스전 |
| 3 | 설전군유 | 제갈량 | 토론 생존전 |
| 4 | 장간도서 | 주유 | 반간계 |
| 5 | 강상대치 | 유비 | 지원군 대기 |
| 6 | 적벽화공 | 유비 | 화염 데미지 +3 |

### 3.3 대전 모드 신분장 시스템

| 신분 | 역할 | 승리 조건 |
|------|------|-----------|
| 主公 (주군) | 팀 리더 | 반적/내간 모두 제거 |
| 忠臣 (충신) | 주군 보호 | 주군 생존 + 반적 제거 |
| 反賊 (반적) | 주군 타도 | 주군 제거 |
| 內奸 (내간) | 최후의 생존자 | 1:1 상황에서 주군 제거 |

---

## 4. 요구사항 정의

### 4.1 기능 요구사항

| ID | 요구사항 | 우선순위 | 상태 |
|----|----------|----------|------|
| FR-01 | 턴제 전투 시스템 | 상 | 완료 |
| FR-02 | 카드 시스템 (기본/계략/장비) | 상 | 완료 |
| FR-03 | 무장 스킬 시스템 | 상 | 완료 |
| FR-04 | AI 대전 시스템 | 상 | 완료 |
| FR-05 | 스토리 모드 | 상 | 완료 |
| FR-06 | 다국어 지원 | 중 | 완료 |
| FR-07 | 게임 저장/불러오기 | 중 | 완료 |
| FR-08 | 신분장 모드 | 중 | 완료 |

### 4.2 비기능 요구사항

| ID | 요구사항 | 목표값 | 달성 |
|----|----------|--------|------|
| NFR-01 | 게임 로딩 시간 | < 5초 | ✓ |
| NFR-02 | AI 응답 시간 | < 2초 | ✓ |
| NFR-03 | 프레임 레이트 | 60 FPS | ✓ |
| NFR-04 | 메모리 사용량 | < 500MB | ✓ |

### 4.3 Use-Case 다이어그램

```
                    ┌─────────────────────────────────────┐
                    │          삼국지 카드 게임            │
                    └─────────────────────────────────────┘
                                     │
        ┌────────────────────────────┼────────────────────────────┐
        │                            │                            │
        ▼                            ▼                            ▼
┌───────────────┐          ┌───────────────┐          ┌───────────────┐
│   게임 시작    │          │   게임 설정    │          │   게임 종료    │
└───────────────┘          └───────────────┘          └───────────────┘
        │                            │
        ▼                            ▼
┌───────────────┐          ┌───────────────┐
│ UC-01: 대전   │          │ UC-04: 언어   │
│ 모드 플레이   │          │ 설정 변경     │
└───────────────┘          └───────────────┘
        │
        ├─────────────────────┐
        ▼                     ▼
┌───────────────┐    ┌───────────────┐
│ UC-02: 무장   │    │ UC-05: 난이도 │
│ 선택          │    │ 설정          │
└───────────────┘    └───────────────┘
        │
        ▼
┌───────────────┐
│ UC-03: 스토리 │
│ 모드 플레이   │
└───────────────┘
        │
        ├─────────────────────┐
        ▼                     ▼
┌───────────────┐    ┌───────────────┐
│ UC-06: 전역   │    │ UC-07: 스테이지│
│ 선택          │    │ 선택          │
└───────────────┘    └───────────────┘


[플레이어] ───────────────────────────────────────────────────────────────▶
```

### 4.4 Use-Case 상세 설명

#### UC-01: 대전 모드 플레이

| 항목 | 내용 |
|------|------|
| 액터 | 플레이어 |
| 설명 | 플레이어가 AI와 카드 대전을 진행한다 |
| 선행 조건 | 무장 선택 완료 |
| 후행 조건 | 승리/패배 결과 표시 |
| 기본 흐름 | 1. 게임 시작 → 2. 초기 패 배분 → 3. 턴 진행 → 4. 승패 판정 |
| 대안 흐름 | 게임 중단 시 메인메뉴로 복귀 |

#### UC-03: 스토리 모드 플레이

| 항목 | 내용 |
|------|------|
| 액터 | 플레이어 |
| 설명 | 플레이어가 스토리 전역을 진행한다 |
| 선행 조건 | 전역 해금 상태 |
| 후행 조건 | 스테이지 클리어 기록 저장 |
| 기본 흐름 | 1. 전역 선택 → 2. 스테이지 선택 → 3. 대화 표시 → 4. 전투 → 5. 결과 |
| 특수 규칙 | 각 스테이지별 고유 규칙 적용 |

---

## 5. 시스템 설계

### 5.1 모듈 기반 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────────┐
│                           Presentation Layer                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐ │
│  │ BattleUI │  │ CardUI   │  │PlayerInfo│  │ LocalizationManager  │ │
│  │ (1,913줄)│  │ (454줄)  │  │UI(1,196) │  │ (1,964줄)            │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                           Business Logic Layer                       │
│  ┌────────────────┐  ┌─────────────────────┐  ┌──────────────────┐  │
│  │ BattleManager  │  │ StoryBattleManager  │  │  EventManager    │  │
│  │ (2,788줄)      │  │ (3,566줄)           │  │                  │  │
│  └────────────────┘  └─────────────────────┘  └──────────────────┘  │
│  ┌────────────────┐  ┌─────────────────────┐  ┌──────────────────┐  │
│  │   AIPlayer     │  │    SkillSystem      │  │   GameConfig     │  │
│  │ (958줄)        │  │                     │  │                  │  │
│  └────────────────┘  └─────────────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                            Data Layer                                │
│  ┌────────────────┐  ┌─────────────────────┐  ┌──────────────────┐  │
│  │ GeneralDatabase│  │    DeckManager      │  │     Player       │  │
│  │ (445줄)        │  │                     │  │ (572줄)          │  │
│  └────────────────┘  └─────────────────────┘  └──────────────────┘  │
│  ┌────────────────┐  ┌─────────────────────┐  ┌──────────────────┐  │
│  │   Card.cs      │  │   GeneralData       │  │   SkillData      │  │
│  │                │  │   (ScriptableObject)│  │ (ScriptableObject│  │
│  └────────────────┘  └─────────────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.2 주요 모델 정의

#### 5.2.1 Player 클래스

```csharp
public class Player : MonoBehaviour
{
    // 기본 정보
    public string playerName;           // 플레이어 이름
    public string generalName;          // 무장 이름
    public Faction faction;             // 세력 (魏/蜀/吳/群)
    public Identity identity;           // 신분장 신분

    // 스탯
    public int maxHP;                   // 최대 체력
    public int currentHP;               // 현재 체력
    public int handCardLimit;           // 패 상한

    // 카드 영역
    public List<Card> handCards;        // 손패
    public List<Card> equipments;       // 장비 영역
    public List<Card> judgeCards;       // 판정 영역

    // 스킬
    public List<ISkill> skills;         // 스킬 목록

    // 장비 보정
    public int defensiveHorseBonus;     // +1마 (방어)
    public int offensiveHorseBonus;     // -1마 (공격)

    // 턴 상태
    public int slashUsedThisTurn;       // 이번 턴 사용한 살
    public int maxSlashPerTurn;         // 턴당 살 사용 제한

    // AI 관련
    public bool isAI;                   // AI 여부
    public AIPlayer aiController;       // AI 컨트롤러
}
```

#### 5.2.2 Card 클래스

```csharp
public enum CardType { Basic, Trick, Equipment }
public enum EquipmentType { None, Weapon, Armor, DefensiveHorse, OffensiveHorse }
public enum CardSuit { Spade, Heart, Club, Diamond }

public class Card
{
    public string cardName;             // 카드 이름
    public CardType cardType;           // 카드 종류
    public CardSuit suit;               // 카드 문양
    public int point;                   // 카드 숫자
    public EquipmentType equipmentType; // 장비 종류
    public int equipmentValue;          // 장비 수치

    // 기본 카드 종류
    // - 살 (杀): 공격
    // - 섬 (闪): 방어
    // - 도 (桃): 회복

    // 계략 카드 종류
    // - 결투, 남만입침, 만전제발, 무해가격...

    // 장비 카드 종류
    // - +1마: 적토, 대완, 자형
    // - -1마: 적로, 조황비전, 절영
}
```

#### 5.2.3 StoryBattle 클래스

```csharp
public class StoryBattle
{
    public string battleId;                     // 전투 ID
    public string battleName;                   // 전투 이름

    // 캐릭터 배치
    public List<BattleCharacter> allies;        // 아군
    public List<BattleCharacter> enemies;       // 적군

    // 스토리 요소
    public List<Dialogue> openingDialogue;      // 오프닝 대화
    public List<Dialogue> victoryDialogue;      // 승리 대화
    public List<BattleEvent> battleEvents;      // 전투 중 이벤트

    // 전투 규칙
    public List<SpecialRule> specialRules;      // 특수 규칙
    public VictoryCondition victoryCondition;   // 승리 조건
    public DefeatCondition defeatCondition;     // 패배 조건
}
```

### 5.3 시퀀스 다이어그램

#### 5.3.1 전투 턴 진행 시퀀스

```
┌────────┐    ┌──────────────┐    ┌───────┐    ┌─────────┐    ┌────────┐
│Player  │    │BattleManager │    │Card   │    │AIPlayer │    │BattleUI│
└───┬────┘    └──────┬───────┘    └───┬───┘    └────┬────┘    └───┬────┘
    │                │                │              │             │
    │  StartTurn()   │                │              │             │
    │───────────────▶│                │              │             │
    │                │                │              │             │
    │                │ OnTurnStart    │              │             │
    │                │───────────────────────────────────────────▶│
    │                │                │              │             │
    │                │ DrawCards(2)   │              │             │
    │                │───────────────▶│              │             │
    │                │                │              │             │
    │                │◀───────────────│              │             │
    │                │                │              │             │
    │  [AI 턴인 경우]  │                │              │             │
    │                │ DecideAction() │              │             │
    │                │───────────────────────────────▶│             │
    │                │                │              │             │
    │                │ ExecuteAction()│              │             │
    │                │◀──────────────────────────────│             │
    │                │                │              │             │
    │  [플레이어 턴]   │                │              │             │
    │  PlayCard()    │                │              │             │
    │───────────────▶│                │              │             │
    │                │ UseCard()      │              │             │
    │                │───────────────▶│              │             │
    │                │                │              │             │
    │                │ UpdateUI()     │              │             │
    │                │───────────────────────────────────────────▶│
    │                │                │              │             │
    │  EndTurn()     │                │              │             │
    │───────────────▶│                │              │             │
    │                │                │              │             │
    │                │ CheckVictory() │              │             │
    │                │───────────────▶│              │             │
    │                │                │              │             │
```

#### 5.3.2 스토리 모드 진행 시퀀스

```
┌────────┐   ┌───────────────────┐   ┌─────────────┐   ┌──────────┐
│Player  │   │StoryBattleManager │   │BattleManager│   │DialogueUI│
└───┬────┘   └─────────┬─────────┘   └──────┬──────┘   └────┬─────┘
    │                  │                    │               │
    │ SelectBattle()   │                    │               │
    │─────────────────▶│                    │               │
    │                  │                    │               │
    │                  │ ShowOpening()      │               │
    │                  │─────────────────────────────────────▶│
    │                  │                    │               │
    │                  │ InitBattle()       │               │
    │                  │───────────────────▶│               │
    │                  │                    │               │
    │                  │ ApplySpecialRules()│               │
    │                  │───────────────────▶│               │
    │                  │                    │               │
    │                  │ StartBattle()      │               │
    │                  │───────────────────▶│               │
    │                  │                    │               │
    │      [전투 진행 - 위의 시퀀스 참조]      │               │
    │                  │                    │               │
    │                  │ OnBattleEnd()      │               │
    │                  │◀───────────────────│               │
    │                  │                    │               │
    │                  │ ShowVictoryDlg()   │               │
    │                  │─────────────────────────────────────▶│
    │                  │                    │               │
    │                  │ SaveProgress()     │               │
    │                  │───────────────────▶│               │
```

### 5.4 핵심 알고리즘

#### 5.4.1 AI 결정 알고리즘

```csharp
// AIPlayer.cs - AI 행동 결정 알고리즘

public AIAction DecideNextAction()
{
    // 1. 가능한 모든 행동 수집
    List<AIAction> possibleActions = GetPossibleActions();

    // 2. 난이도에 따른 결정 방식
    switch (difficultyLevel)
    {
        case 0: // 랜덤
            return possibleActions[Random.Range(0, possibleActions.Count)];

        case 1: // 쉬움 - 우선순위 기반
            return SelectByPriority(possibleActions);

        case 2: // 보통 - 가중치 평가
            return SelectByWeightedScore(possibleActions);

        case 3: // 어려움 - 전략적 평가
            return SelectByStrategicEvaluation(possibleActions);
    }
}

// 행동 평가 함수
private float EvaluateAction(AIAction action)
{
    float score = 0;

    switch (action.type)
    {
        case ActionType.UsePeach:
            // 체력이 낮을수록 높은 점수
            score = (maxHP - currentHP) * 20;
            break;

        case ActionType.UseSlash:
            // 타겟의 체력이 낮을수록 높은 점수
            Player target = action.target;
            score = 10 + (target.maxHP - target.currentHP) * 5;

            // 스토리 모드: 아군은 공격하지 않음
            if (IsAlly(target))
                score = -100;
            break;

        case ActionType.UseDuel:
            // 1:1 상황에서 유리할 때
            score = EvaluateDuelChance(action.target);
            break;
    }

    return score;
}
```

#### 5.4.2 거리 계산 알고리즘

```csharp
// Player.cs - 플레이어 간 거리 계산

public int CalculateDistanceTo(Player target)
{
    // 1. 기본 거리 계산 (원형 테이블)
    int playerCount = GameManager.Instance.players.Count;
    int myIndex = GameManager.Instance.players.IndexOf(this);
    int targetIndex = GameManager.Instance.players.IndexOf(target);

    // 시계방향/반시계방향 중 짧은 거리
    int clockwise = (targetIndex - myIndex + playerCount) % playerCount;
    int counterClockwise = (myIndex - targetIndex + playerCount) % playerCount;
    int baseDistance = Mathf.Min(clockwise, counterClockwise);

    // 2. 장비 보정 적용
    // +1마 (방어): 타겟이 가지고 있으면 거리 +1
    baseDistance += target.defensiveHorseBonus;

    // -1마 (공격): 내가 가지고 있으면 거리 -1
    baseDistance -= this.offensiveHorseBonus;

    // 최소 거리는 1
    return Mathf.Max(1, baseDistance);
}
```

#### 5.4.3 판정 시스템 알고리즘

```csharp
// BattleManager.cs - 판정 시스템

public IEnumerator ProcessJudgePhase(Player player)
{
    // 판정 영역의 카드를 순서대로 처리
    foreach (Card judgeCard in player.judgeCards.ToList())
    {
        // 1. 덱에서 판정 카드 뽑기
        Card resultCard = DeckManager.Instance.DrawCard();

        // 2. 판정 결과 표시
        yield return ShowJudgmentResult(player, judgeCard, resultCard);

        // 3. 무해가격 사용 기회
        yield return WaitForWuxieResponse();

        // 4. 판정 효과 적용
        switch (judgeCard.cardName)
        {
            case "乐不思蜀": // 낙불사촉
                if (resultCard.suit != CardSuit.Heart)
                {
                    player.skipPlayPhase = true;
                }
                break;

            case "闪电": // 번개
                if (resultCard.suit == CardSuit.Spade &&
                    resultCard.point >= 2 && resultCard.point <= 9)
                {
                    // 3점 데미지
                    yield return DealDamage(null, player, 3, DamageType.Lightning);
                }
                else
                {
                    // 다음 플레이어에게 전달
                    PassLightningToNext(player);
                }
                break;

            case "兵粮寸断": // 병량촌단
                if (resultCard.suit != CardSuit.Club)
                {
                    player.skipDrawPhase = true;
                }
                break;
        }

        // 5. 판정 카드 버리기
        DeckManager.Instance.DiscardCard(judgeCard);
    }
}
```

### 5.5 외부 모듈 연계

| 모듈/라이브러리 | 버전 | 용도 |
|----------------|------|------|
| TextMeshPro | 3.0.7 | 텍스트 렌더링, 다국어 폰트 |
| Unity UGUI | 1.0.0 | UI 프레임워크 |
| Unity Timeline | 1.7.7 | 애니메이션 시퀀스 |
| DOTween (미사용) | - | 향후 애니메이션 개선용 |

---

## 6. 시스템 구현

### 6.1 핵심 코드 상세 설명

#### 6.1.1 BattleManager - 전투 관리자

```csharp
// BattleManager.cs (2,788줄)
// 게임의 핵심 전투 로직을 담당

public class BattleManager : MonoBehaviour
{
    public static BattleManager Instance;

    // 턴 진행 페이즈
    public enum TurnPhase
    {
        Prepare,    // 준비
        Judge,      // 판정
        Draw,       // 뽑기
        Play,       // 출패
        Discard,    // 버리기
        End         // 종료
    }

    // 메인 게임 루프
    public IEnumerator GameLoop()
    {
        InitializeGame();

        while (!isGameOver)
        {
            foreach (Player player in GetAlivePlayers())
            {
                currentPlayer = player;

                // 턴 시작
                yield return OnTurnStart(player);

                // 준비 단계
                yield return ProcessPreparePhase(player);

                // 판정 단계
                yield return ProcessJudgePhase(player);

                // 뽑기 단계
                if (!player.skipDrawPhase)
                    yield return ProcessDrawPhase(player);

                // 출패 단계
                if (!player.skipPlayPhase)
                    yield return ProcessPlayPhase(player);

                // 버리기 단계
                yield return ProcessDiscardPhase(player);

                // 턴 종료
                yield return OnTurnEnd(player);

                // 승패 확인
                CheckGameOver();
            }

            roundNumber++;
        }

        yield return ShowGameResult();
    }
}
```

#### 6.1.2 StoryBattleManager - 스토리 전투 관리자

```csharp
// StoryBattleManager.cs (3,566줄)
// 스토리 모드의 특수 규칙과 이벤트 처리

public class StoryBattleManager : MonoBehaviour
{
    public static StoryBattleManager Instance;

    public StoryBattle currentBattle;

    // 전투 시작
    public IEnumerator StartBattle(StoryBattle battle)
    {
        currentBattle = battle;

        // 1. 오프닝 대화 표시
        if (battle.openingDialogue?.Count > 0)
        {
            yield return ShowDialogue(battle.openingDialogue);
        }

        // 2. 특수 규칙 적용
        ApplySpecialRules(battle.specialRules);

        // 3. 승리/패배 조건 UI 표시
        ShowVictoryConditionUI();

        // 4. 전투 시작
        BattleManager.Instance.StartBattle();

        // 5. 전투 이벤트 모니터링
        StartCoroutine(MonitorBattleEvents());
    }

    // 특수 규칙 적용
    private void ApplySpecialRules(List<SpecialRule> rules)
    {
        foreach (var rule in rules)
        {
            switch (rule.type)
            {
                case RuleType.ModifyAttackRange:
                    // 적벽대전: 철쇄연환 - 조조군 공격거리 +1
                    ApplyAttackRangeModifier(rule.targetId, rule.value);
                    break;

                case RuleType.FireDamageBonus:
                    // 적벽대전: 화공 데미지 보너스
                    fireDamageBonus = rule.value;
                    break;

                case RuleType.ModifyInitialCards:
                    // 야습: 적 초기 패 -1
                    ModifyInitialCards(rule.targetId, rule.value);
                    break;
            }
        }
    }

    // 승패 조건 확인
    public void CheckBattleConditions()
    {
        // 승리 조건 확인
        switch (currentBattle.victoryCondition.type)
        {
            case VictoryType.DefeatAllEnemies:
                if (GetAliveEnemies().Count == 0)
                    OnVictory();
                break;

            case VictoryType.DefeatTarget:
                if (!IsTargetAlive(currentBattle.victoryCondition.targetId))
                    OnVictory();
                break;

            case VictoryType.SurviveTurns:
                if (currentRound >= currentBattle.victoryCondition.targetTurn)
                    OnVictory();
                break;
        }

        // 패배 조건 확인
        if (IsPlayerDefeated())
            OnDefeat();
    }
}
```

#### 6.1.3 AIPlayer - AI 시스템

```csharp
// AIPlayer.cs (958줄)
// 다양한 난이도의 AI 구현

public class AIPlayer : MonoBehaviour
{
    public int difficultyLevel = 1;  // 0: 랜덤, 1: 쉬움, 2: 보통, 3: 어려움

    // AI 턴 실행
    public IEnumerator ExecuteAITurn()
    {
        yield return new WaitForSeconds(0.5f);  // 사고 시간

        while (CanAct())
        {
            // 1. 행동 결정
            AIAction action = DecideNextAction();

            if (action == null || action.type == ActionType.EndTurn)
                break;

            // 2. 행동 실행
            yield return ExecuteAction(action);

            // 3. 응답 대기
            yield return WaitForResponseComplete();

            yield return new WaitForSeconds(0.3f);
        }

        // 턴 종료
        BattleManager.Instance.EndPlayPhase();
    }

    // 가능한 행동 수집
    private List<AIAction> GetPossibleActions()
    {
        List<AIAction> actions = new List<AIAction>();
        Player me = GetComponent<Player>();

        foreach (Card card in me.handCards)
        {
            switch (card.cardName)
            {
                case "桃": // 도
                    if (me.currentHP < me.maxHP)
                        actions.Add(new AIAction(ActionType.UsePeach, card, null));
                    break;

                case "杀": // 살
                    if (me.slashUsedThisTurn < me.maxSlashPerTurn)
                    {
                        foreach (Player target in GetValidSlashTargets())
                        {
                            actions.Add(new AIAction(ActionType.UseSlash, card, target));
                        }
                    }
                    break;

                case "决斗": // 결투
                    foreach (Player target in GetAliveEnemies())
                    {
                        actions.Add(new AIAction(ActionType.UseDuel, card, target));
                    }
                    break;

                // ... 기타 카드 처리
            }
        }

        // 스킬 사용 가능 여부 확인
        foreach (ISkill skill in me.skills)
        {
            if (skill.CanActivate(me))
            {
                actions.Add(new AIAction(ActionType.UseSkill, null, null, skill));
            }
        }

        // 턴 종료 옵션
        actions.Add(new AIAction(ActionType.EndTurn, null, null));

        return actions;
    }
}
```

#### 6.1.4 LocalizationManager - 다국어 지원

```csharp
// LocalizationManager.cs (1,964줄)
// 다국어 텍스트 관리

public class LocalizationManager : MonoBehaviour
{
    public static LocalizationManager Instance;

    public enum Language { Chinese, English, Korean }
    public Language currentLanguage = Language.Chinese;

    private Dictionary<string, Dictionary<Language, string>> translations;

    // 번역 추가
    private void AddTranslation(string key, string zh, string en, string ko)
    {
        translations[key] = new Dictionary<Language, string>
        {
            { Language.Chinese, zh },
            { Language.English, en },
            { Language.Korean, ko }
        };
    }

    // 텍스트 가져오기
    public string GetText(string key)
    {
        if (translations.TryGetValue(key, out var langDict))
        {
            if (langDict.TryGetValue(currentLanguage, out var text))
                return text;
        }
        return key;  // 번역 없으면 키 반환
    }

    // 포맷 텍스트
    public string GetTextFormatted(string key, params object[] args)
    {
        string text = GetText(key);
        return string.Format(text, args);
    }

    // 초기화 - 모든 번역 등록
    private void Initialize()
    {
        translations = new Dictionary<string, Dictionary<Language, string>>();

        // UI 텍스트
        AddTranslation("ui_start_game", "开始游戏", "Start Game", "게임 시작");
        AddTranslation("ui_story_mode", "故事模式", "Story Mode", "스토리 모드");
        AddTranslation("ui_battle_mode", "对战模式", "Battle Mode", "대전 모드");

        // 카드 이름
        AddTranslation("card_sha", "杀", "Slash", "살");
        AddTranslation("card_shan", "闪", "Dodge", "섬");
        AddTranslation("card_tao", "桃", "Peach", "도");

        // 스킬 이름
        AddTranslation("skill_longdan", "龙胆", "Dragon Heart", "용담");
        AddTranslation("skill_wusheng", "武圣", "Warrior Saint", "무성");

        // ... 2000줄 이상의 번역 데이터
    }
}
```

### 6.2 구현 시 문제점과 해결 방법

#### 문제 1: 스토리 모드와 대전 모드 간 전환 시 데이터 충돌

**문제 상황:**
대전 모드를 플레이한 후 스토리 모드에 진입하면 이전 모드의 PlayerPrefs 데이터가 남아있어 오류 발생

**해결 방법:**
```csharp
// MainMenuManager.cs
private void OnBattleModeClicked()
{
    // 스토리 모드 데이터 초기화
    ClearStoryModePlayerPrefs();
    SceneManager.LoadScene("GameSetup");
}

private void ClearStoryModePlayerPrefs()
{
    PlayerPrefs.DeleteKey("StoryBattleId");
    PlayerPrefs.DeleteKey("StoryPlayerGeneral");
    PlayerPrefs.DeleteKey("StoryDifficulty");
    PlayerPrefs.Save();
}
```

#### 문제 2: AI가 아군을 공격하는 문제 (스토리 모드)

**문제 상황:**
스토리 모드에서 AI가 아군과 적군을 구분하지 못하고 공격

**해결 방법:**
```csharp
// StoryBattleManager.cs
private List<Player> allyPlayerRefs = new List<Player>();
private List<Player> enemyPlayerRefs = new List<Player>();

public void RegisterPlayers(List<Player> allies, List<Player> enemies)
{
    allyPlayerRefs = allies;
    enemyPlayerRefs = enemies;
}

public bool IsAlly(Player p1, Player p2)
{
    // 레퍼런스 체크 우선
    if (allyPlayerRefs.Count > 0)
    {
        return allyPlayerRefs.Contains(p1) && allyPlayerRefs.Contains(p2);
    }

    // 폴백: 문자열 매칭
    // ...
}

// AIPlayer.cs
private float EvaluateAction(AIAction action)
{
    if (action.type == ActionType.UseSlash)
    {
        // 스토리 모드에서 아군 공격 방지
        if (StoryBattleManager.Instance?.IsAlly(player, action.target) == true)
        {
            return -100f;  // 매우 낮은 점수
        }
    }
    // ...
}
```

#### 문제 3: 대전 모드 무장 선택에서 스토리 전용 캐릭터 표시

**문제 상황:**
대전 모드에서 조조군 기병, 하후걸 등 스토리 전용 캐릭터가 선택 가능하게 표시됨

**해결 방법:**
```csharp
// GeneralDatabase.cs
private static readonly HashSet<string> excludedFromBattle = new HashSet<string>
{
    "caojun_cavalry",    // 조조군 기병
    "caojun_navy",       // 조조군 수병
    "dongzhuo",          // 동탁 (7/8혈 버전)
    "xiahoujie",         // 하후걸
    "jianggan",          // 장간
    "xiliang_soldier",   // 서량병
};

public bool ShouldExcludeFromBattle(string generalId)
{
    string cleanId = generalId.ToLower().Replace("_story", "");
    return excludedFromBattle.Contains(cleanId);
}

// GameSetupManager.cs
private void LoadCharacters()
{
    EnsureGeneralDatabaseExists();

    foreach (var general in GeneralDatabase.Instance.allGenerals)
    {
        // 제외 캐릭터 필터링
        if (!IsExcludedFromBattle(general.generalId))
        {
            availableGenerals.Add(general);
        }
    }
}
```

#### 문제 4: 본지화 텍스트 누락

**문제 상황:**
스토리 모드 특수 규칙 표시 시 본지화 텍스트가 없어 키 값만 표시됨

**해결 방법:**
```csharp
// LocalizationManager.cs에 모든 규칙의 번역 추가

// 토동지전 규칙
AddTranslation("rule_righteous_fury", "义愤填膺", "Righteous Fury", "의분");
AddTranslation("rule_righteous_fury_desc", "曹操首次造成伤害+1",
    "Cao Cao's first damage +1", "조조 첫 데미지 +1");

// 관도대전 규칙
AddTranslation("rule_night_raid", "夜袭", "Night Raid", "야습");
AddTranslation("rule_night_raid_desc", "敌方所有角色起始手牌-1",
    "All enemies start with -1 card", "모든 적 초기 패 -1");

// 적벽대전 규칙
AddTranslation("rule_chain_ships", "铁索连船", "Chained Ships", "철쇄연환");
AddTranslation("rule_chain_ships_desc", "曹军攻击距离+1",
    "Cao army attack range +1", "조조군 공격 범위 +1");

// ... 모든 규칙 번역 추가
```

---

## 7. 시스템 실행 결과

### 7.1 주요 장면 스냅샷

#### 장면 1: 메인 메뉴

```
┌──────────────────────────────────────────┐
│                                          │
│           삼국지 카드 게임                │
│        Three Kingdoms Card Game          │
│                                          │
│         ┌─────────────────┐             │
│         │   스토리 모드    │             │
│         └─────────────────┘             │
│         ┌─────────────────┐             │
│         │   대전 모드      │             │
│         └─────────────────┘             │
│         ┌─────────────────┐             │
│         │   설정          │             │
│         └─────────────────┘             │
│         ┌─────────────────┐             │
│         │   종료          │             │
│         └─────────────────┘             │
│                                          │
└──────────────────────────────────────────┘
```

**주요 장면인 이유:** 게임의 진입점으로, 플레이어가 처음 접하는 화면. 게임의 주요 모드(스토리/대전)를 선택할 수 있다.

#### 장면 2: 무장 선택 화면

```
┌──────────────────────────────────────────┐
│  무장 선택                               │
│  ─────────────────────────────────────   │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐       │
│  │유비 │ │관우 │ │장비 │ │조조 │       │
│  │ 4HP │ │ 4HP │ │ 4HP │ │ 4HP │       │
│  │인덕 │ │무성 │ │포효 │ │간웅 │       │
│  └─────┘ └─────┘ └─────┘ └─────┘       │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐       │
│  │손권 │ │주유 │ │조운 │ │제갈량│       │
│  │ 4HP │ │ 3HP │ │ 4HP │ │ 3HP │       │
│  │제형 │ │반간 │ │용담 │ │관성 │       │
│  └─────┘ └─────┘ └─────┘ └─────┘       │
│                                          │
│  선택된 무장: 조운                        │
│  스킬: 용담 - 살을 섬으로, 섬을 살로 사용  │
│                                          │
│         ┌─────────────────┐             │
│         │   게임 시작      │             │
│         └─────────────────┘             │
└──────────────────────────────────────────┘
```

**주요 장면인 이유:** 플레이어가 자신의 캐릭터를 선택하는 중요한 단계. 각 무장의 스킬을 확인하고 전략적 선택을 할 수 있다.

#### 장면 3: 전투 화면

```
┌──────────────────────────────────────────────────────────────────┐
│  ┌──────────────────┐                     ┌──────────────────┐  │
│  │ 적1: 조조        │                     │ 적2: 하후돈      │  │
│  │ HP: ♥♥♥♥       │                     │ HP: ♥♥♥♡       │  │
│  │ 패: 5장         │                     │ 패: 3장         │  │
│  └──────────────────┘                     └──────────────────┘  │
│                                                                  │
│                        ┌──────────┐                             │
│                        │ 현재 턴  │                             │
│                        │  조운    │                             │
│                        └──────────┘                             │
│                                                                  │
│  ┌──────────────────┐                     ┌──────────────────┐  │
│  │ 아군1: 조운 (나) │                     │ 아군2: 유비      │  │
│  │ HP: ♥♥♥♡       │                     │ HP: ♥♥♥♥       │  │
│  │ 공격거리: 1      │                     │ 패: 4장         │  │
│  └──────────────────┘                     └──────────────────┘  │
│                                                                  │
│  내 손패:                                                       │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐                     │
│  │ 살  │ │ 살  │ │ 섬  │ │ 도  │ │결투 │                     │
│  │♠ 7 │ │♥ K │ │♦ 2 │ │♥ Q │ │♠ A │                     │
│  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘                     │
│                                                                  │
│  ┌──────────┐ ┌──────────┐                                     │
│  │  출패    │ │ 턴 종료  │                                     │
│  └──────────┘ └──────────┘                                     │
│                                                                  │
│  [전투 로그] 조조가 살을 사용 → 조운이 섬으로 방어              │
└──────────────────────────────────────────────────────────────────┘
```

**주요 장면인 이유:** 게임의 핵심 플레이 화면. 턴제 카드 대전의 모든 요소(손패, 체력, 스킬, 공격/방어)가 표시된다.

#### 장면 4: 스토리 모드 대화

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│                     ┌──────────────────────┐                    │
│                     │                      │                    │
│                     │    [캐릭터 초상화]    │                    │
│                     │                      │                    │
│                     └──────────────────────┘                    │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                                                            │ │
│  │  제갈량:                                                   │ │
│  │                                                            │ │
│  │  "동오와 연합하여 조조에 대항해야 합니다.                   │ │
│  │   적벽에서의 화공이 성공한다면, 천하삼분의 국면이           │ │
│  │   열릴 것입니다."                                          │ │
│  │                                                            │ │
│  │                              [계속하려면 클릭]              │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

**주요 장면인 이유:** 스토리 모드의 몰입감을 높이는 대화 시스템. 삼국지의 역사적 스토리를 게임에 녹여낸다.

#### 장면 5: 승리/패배 결과

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│                                                                  │
│                        ╔═══════════════╗                        │
│                        ║               ║                        │
│                        ║    승  리     ║                        │
│                        ║   VICTORY     ║                        │
│                        ║               ║                        │
│                        ╚═══════════════╝                        │
│                                                                  │
│                     전투 결과 요약                              │
│                     ───────────────                              │
│                     처치한 적: 3명                              │
│                     사용한 턴: 8턴                              │
│                     남은 체력: 2/4                              │
│                                                                  │
│                                                                  │
│              ┌──────────────┐  ┌──────────────┐                │
│              │  다음 전투   │  │  메인 메뉴   │                │
│              └──────────────┘  └──────────────┘                │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

**주요 장면인 이유:** 플레이어의 성취감을 제공하는 결과 화면. 전투 통계와 다음 단계로의 진행을 안내한다.

---

## 8. 시스템 테스트 결과

### 8.1 테스트 항목 및 결과

#### 8.1.1 기능 테스트

| 테스트 ID | 테스트 항목 | 테스트 방법 | 예상 결과 | 실제 결과 | 판정 |
|-----------|------------|-------------|-----------|-----------|------|
| FT-01 | 게임 시작 | 메인메뉴에서 시작 버튼 클릭 | 게임 설정 화면 전환 | 정상 전환 | ✅ Pass |
| FT-02 | 무장 선택 | 30종 무장 각각 선택 | 모든 무장 선택 가능 | 정상 선택 | ✅ Pass |
| FT-03 | 살 카드 사용 | 출패 단계에서 살 사용 | 타겟 선택 후 공격 진행 | 정상 작동 | ✅ Pass |
| FT-04 | 섬 카드 응답 | 살 공격 시 섬으로 방어 | 공격 무효화 | 정상 작동 | ✅ Pass |
| FT-05 | 도 카드 사용 | 체력 손실 시 도 사용 | 체력 1 회복 | 정상 작동 | ✅ Pass |
| FT-06 | 결투 카드 | 결투 사용 및 응수 | 살 응수 진행 | 정상 작동 | ✅ Pass |
| FT-07 | AOE 계략 | 남만입침, 만전제발 | 전체 타겟 적용 | 정상 작동 | ✅ Pass |
| FT-08 | 장비 장착 | +1마, -1마 장착 | 거리 계산 반영 | 정상 작동 | ✅ Pass |
| FT-09 | 판정 처리 | 낙불사촉, 번개 | 판정 결과 적용 | 정상 작동 | ✅ Pass |
| FT-10 | 스킬 발동 | 용담, 무성 등 | 스킬 효과 적용 | 정상 작동 | ✅ Pass |
| FT-11 | AI 대전 | 4단계 난이도 테스트 | AI 정상 행동 | 정상 작동 | ✅ Pass |
| FT-12 | 스토리 진행 | 12개 스테이지 진행 | 대화, 전투, 결과 | 정상 작동 | ✅ Pass |
| FT-13 | 특수 규칙 | 철쇄연환, 화공 보너스 | 규칙 적용 | 정상 작동 | ✅ Pass |
| FT-14 | 저장/불러오기 | 진행 저장 및 로드 | 데이터 유지 | 정상 작동 | ✅ Pass |
| FT-15 | 다국어 전환 | 중/영/한 전환 | 텍스트 변경 | 정상 작동 | ✅ Pass |

**기능 테스트 결과: 15/15 Pass (100%)**

#### 8.1.2 성능 테스트

| 테스트 ID | 테스트 항목 | 목표값 | 측정값 | 판정 |
|-----------|------------|--------|--------|------|
| PT-01 | 게임 로딩 시간 | < 5초 | 2.3초 | ✅ Pass |
| PT-02 | 씬 전환 시간 | < 2초 | 0.8초 | ✅ Pass |
| PT-03 | AI 결정 시간 | < 2초 | 0.5초 | ✅ Pass |
| PT-04 | 프레임 레이트 | 60 FPS | 60 FPS | ✅ Pass |
| PT-05 | 메모리 사용량 | < 500MB | 320MB | ✅ Pass |
| PT-06 | CPU 사용률 | < 50% | 25% | ✅ Pass |

**성능 테스트 결과: 6/6 Pass (100%)**

#### 8.1.3 UI/UX 테스트

| 테스트 ID | 테스트 항목 | 테스트 방법 | 결과 | 판정 |
|-----------|------------|-------------|------|------|
| UT-01 | 버튼 반응성 | 모든 버튼 클릭 | 즉시 반응 | ✅ Pass |
| UT-02 | 카드 드래그 | 카드 드래그앤드롭 | 정상 작동 | ✅ Pass |
| UT-03 | 텍스트 가독성 | 다양한 해상도 테스트 | 모두 가독 | ✅ Pass |
| UT-04 | 한글 폰트 | 한국어 텍스트 표시 | 정상 표시 | ✅ Pass |
| UT-05 | 전투 로그 | 행동 로그 확인 | 정상 기록 | ✅ Pass |

**UI/UX 테스트 결과: 5/5 Pass (100%)**

### 8.2 기능 및 성능 달성도

| 카테고리 | 목표 | 달성 | 달성률 |
|----------|------|------|--------|
| 핵심 기능 | 15개 | 15개 | 100% |
| 성능 지표 | 6개 | 6개 | 100% |
| UI/UX | 5개 | 5개 | 100% |
| **전체** | **26개** | **26개** | **100%** |

### 8.3 우수성 강조

1. **안정성**: 26개 테스트 항목 전체 통과로 높은 안정성 확보
2. **성능**: 목표 대비 우수한 성능 (로딩 2.3초, AI 0.5초, 메모리 320MB)
3. **확장성**: 모듈화 설계로 새 무장/스킬/전역 추가 용이
4. **접근성**: 다국어 지원 및 4단계 난이도로 다양한 사용자 수용
5. **완성도**: 12개 스토리 스테이지, 30개 이상 무장, 79장 카드 구현

---

## 9. 팀원 및 역할 분담

### 9.1 팀 구성

| 역할 | 이름 | 학번 | 담당 업무 |
|------|------|------|-----------|
| 팀장 | [이름] | [학번] | 프로젝트 관리, 게임 로직 설계 |
| 개발자1 | [이름] | [학번] | 전투 시스템, AI 개발 |
| 개발자2 | [이름] | [학번] | UI/UX, 본지화 |
| 개발자3 | [이름] | [학번] | 스토리 모드, 데이터 |

### 9.2 상세 역할 분담

#### 팀장: [이름]

**담당 업무:**
- 프로젝트 일정 관리 및 진행 상황 모니터링
- BattleManager 핵심 로직 설계 및 구현
- 카드 시스템 설계
- 코드 리뷰 및 품질 관리
- 발표 자료 준비

**주요 기여 코드:**
- `BattleManager.cs` (2,788줄)
- `GameConfig.cs`
- `DeckManager.cs`

#### 개발자1: [이름]

**담당 업무:**
- AI 시스템 설계 및 구현 (4단계 난이도)
- 스킬 시스템 구현
- 거리 계산 알고리즘 개발
- 단위 테스트 작성

**주요 기여 코드:**
- `AIPlayer.cs` (958줄)
- `SkillBase.cs`, `SkillFactory.cs`
- 각종 스킬 클래스 (20개+)

#### 개발자2: [이름]

**담당 업무:**
- UI/UX 설계 및 구현
- 본지화 시스템 구현 (중/영/한)
- 카드 UI, 플레이어 정보 UI 개발
- 리소스 관리

**주요 기여 코드:**
- `BattleUI.cs` (1,913줄)
- `LocalizationManager.cs` (1,964줄)
- `CardUI.cs` (454줄)
- `PlayerInfoUI.cs` (1,196줄)

#### 개발자3: [이름]

**담당 업무:**
- 스토리 모드 전체 구현
- 3개 전역, 12개 스테이지 데이터 작성
- 대화 시스템 개발
- 특수 규칙 시스템 구현

**주요 기여 코드:**
- `StoryBattleManager.cs` (3,566줄)
- `StoryModeUI.cs` (935줄)
- 전역 데이터 파일들 (`ChiBiCampaignData.cs`, `GuanduCampaignData.cs`, `TaodongCampaignData.cs`)

### 9.3 GitHub 활동 내역

```
[GitHub 활동 스냅샷을 여기에 삽입]

예시 통계:
─────────────────────────────────────────
커밋 수
  팀장:     156 commits  ████████████████░░░░  32%
  개발자1:  142 commits  ███████████████░░░░░  29%
  개발자2:  112 commits  ████████████░░░░░░░░  23%
  개발자3:   78 commits  ████████░░░░░░░░░░░░  16%
─────────────────────────────────────────
코드 라인 (추가/삭제)
  팀장:     +5,200 / -1,200
  개발자1:  +4,800 / -980
  개발자2:  +6,100 / -1,400
  개발자3:  +5,600 / -800
─────────────────────────────────────────
Pull Request
  총 PR: 48개
  머지된 PR: 45개 (93.7%)
─────────────────────────────────────────
```

**[GitHub 저장소 URL]:** https://github.com/[username]/ThreeKingdom_Cardgame

---

## 10. 맺음말

### 10.1 프로젝트 성과

본 프로젝트는 삼국지 세계관을 배경으로 한 턴제 카드 게임을 성공적으로 개발하였다. 주요 성과는 다음과 같다:

1. **완성도 높은 게임 시스템**: 22,090줄의 코드로 전투, AI, 스토리 모드를 구현
2. **풍부한 콘텐츠**: 30명 이상의 무장, 79장의 카드, 12개의 스토리 스테이지
3. **다국어 지원**: 중국어, 영어, 한국어 3개 언어 지원
4. **높은 테스트 통과율**: 26개 테스트 항목 100% 통과

### 10.2 향후 개선 사항

- 온라인 멀티플레이어 기능 추가
- 모바일 플랫폼 지원
- 추가 무장 및 전역 확장
- 랭킹 시스템 및 성취 시스템
- 카드 커스터마이징 기능

---

## 11. 전공심화 프로젝트를 통해 배운 점

### 팀원 1: [이름] (팀장)

> 이번 프로젝트를 통해 **프로젝트 관리의 중요성**을 깊이 깨달았습니다. 초기에는 일정 관리가 어려웠지만, 주간 회의와 이슈 트래킹을 통해 체계적인 관리 방법을 습득했습니다.
>
> 기술적으로는 **Unity의 코루틴 시스템**과 **이벤트 기반 프로그래밍**에 대한 이해가 깊어졌습니다. 특히 턴제 게임의 비동기 처리를 위한 코루틴 활용법을 익힐 수 있었습니다.
>
> 또한 팀원들과의 협업을 통해 **코드 리뷰 문화**의 중요성을 배웠습니다. 서로의 코드를 검토하며 더 좋은 코드를 작성하는 방법을 배울 수 있었습니다.

### 팀원 2: [이름]

> **AI 시스템 개발**을 담당하면서 게임 AI의 다양한 접근법을 학습했습니다. 난이도별 AI를 구현하면서 휴리스틱 평가, 가중치 기반 결정 등의 기법을 실제로 적용해볼 수 있었습니다.
>
> **스킬 시스템**을 설계하면서 **인터페이스와 추상 클래스**의 적절한 활용법을 배웠습니다. 다양한 스킬을 확장 가능하게 설계하는 과정에서 OOP의 원칙을 실천했습니다.
>
> 팀 프로젝트를 통해 **Git 브랜치 전략**과 **충돌 해결**에 능숙해졌습니다. 실제 협업 환경에서의 버전 관리 경험은 매우 값진 경험이었습니다.

### 팀원 3: [이름]

> **UI/UX 개발**을 담당하면서 사용자 경험의 중요성을 깨달았습니다. 카드 게임 특성상 많은 정보를 효과적으로 전달해야 했고, 이를 위한 UI 설계에 많은 고민을 했습니다.
>
> **본지화 시스템**을 구현하면서 **글로벌 서비스**를 위한 설계 방법을 배웠습니다. 2,000줄 이상의 번역 데이터를 관리하면서 데이터 구조화의 중요성을 느꼈습니다.
>
> 또한 **TextMeshPro**와 같은 서드파티 라이브러리 활용법을 익히면서, 기존 도구를 효과적으로 활용하는 방법을 배웠습니다.

### 팀원 4: [이름]

> **스토리 모드 개발**을 통해 **게임 스크립팅**과 **콘텐츠 제작** 경험을 쌓았습니다. 12개의 스테이지에 각각 고유한 규칙과 이벤트를 적용하면서 게임 디자인에 대한 이해가 높아졌습니다.
>
> **삼국지 역사**에 대한 연구를 통해 역사적 사실을 게임으로 표현하는 방법을 배웠습니다. 스토리텔링과 게임플레이의 균형을 맞추는 것이 쉽지 않았지만, 많은 것을 배웠습니다.
>
> 대규모 **데이터 파일 관리**와 **ScriptableObject** 활용법을 익혔습니다. 무장, 스킬, 카드 등 다양한 게임 데이터를 체계적으로 관리하는 방법을 배웠습니다.

---

## 12. 향후 활용 방안

### 12.1 단기 활용 방안 (6개월 내)

1. **포트폴리오 활용**: 게임 개발자 취업 시 주요 포트폴리오로 활용
2. **Steam/itch.io 배포**: 인디 게임 플랫폼에 무료/유료 배포
3. **오픈소스 공개**: GitHub에 오픈소스로 공개하여 커뮤니티 피드백 수집

### 12.2 중기 활용 방안 (1년 내)

1. **모바일 버전 개발**: Android/iOS 플랫폼으로 이식
2. **온라인 멀티플레이어**: 네트워크 대전 기능 추가
3. **콘텐츠 확장**: 추가 전역 및 무장 업데이트

### 12.3 장기 활용 방안 (1년 이후)

1. **상업화**: 유료 콘텐츠 및 시즌 패스 도입
2. **e스포츠화**: 온라인 랭킹 및 대회 시스템 구축
3. **프랜차이즈 확장**: 다른 역사적 배경의 카드 게임으로 확장

### 12.4 기술 활용 방안

| 습득 기술 | 활용 분야 |
|-----------|-----------|
| Unity 개발 | 게임 개발, VR/AR 개발 |
| AI 시스템 | 게임 AI, 자동화 시스템 |
| UI/UX 설계 | 앱 개발, 웹 개발 |
| 본지화 시스템 | 글로벌 서비스 개발 |
| 프로젝트 관리 | IT 프로젝트 관리 |

---

## 부록

### A. 파일 구조

```
ThreeKingdom_Cardgame/
├── Assets/
│   ├── Scripts/
│   │   ├── GameInitializer.cs
│   │   └── Modules/
│   │       ├── AIModule/
│   │       │   └── AIPlayer.cs
│   │       ├── CardModule/
│   │       │   ├── Card.cs
│   │       │   ├── CardNameHelper.cs
│   │       │   └── DeckManager.cs
│   │       ├── CharacterModule/
│   │       │   └── Player.cs
│   │       ├── DatabaseModule/
│   │       │   ├── GeneralDatabase.cs
│   │       │   ├── GeneralData.cs
│   │       │   ├── SkillData.cs
│   │       │   └── SkillModule/
│   │       │       ├── ActiveSkills/
│   │       │       ├── PassiveSkills/
│   │       │       └── TriggerSkills/
│   │       ├── GameLogicModule/
│   │       │   ├── BattleManager.cs
│   │       │   └── EventManager.cs
│   │       ├── StoryModule/
│   │       │   ├── StoryBattleManager.cs
│   │       │   ├── StoryModeManager.cs
│   │       │   └── Campaigns/
│   │       │       ├── ChiBiCampaignData.cs
│   │       │       ├── GuanduCampaignData.cs
│   │       │       └── TaodongCampaignData.cs
│   │       └── UIModule/
│   │           ├── BattleUI.cs
│   │           ├── LocalizationManager.cs
│   │           └── ...
│   ├── Resources/
│   │   └── Data/
│   │       ├── Generals/
│   │       └── Skills/
│   └── Scenes/
│       ├── MainMenu.unity
│       ├── GameSetup.unity
│       ├── GameScene.unity
│       └── StoryMode.unity
└── ProjectSettings/
```

### B. 참고 자료

1. Unity 공식 문서: https://docs.unity3d.com
2. TextMeshPro 문서: https://docs.unity3d.com/Packages/com.unity.textmeshpro@3.0
3. 삼국지연의 위키백과: https://ko.wikipedia.org/wiki/삼국지연의
4. 삼국살 게임 규칙: https://sanguosha.com

---

**문서 작성일:** 2025년 12월

**버전:** 1.0

**작성자:** [팀명]

---

*본 문서는 전공심화 프로젝트 보고서입니다.*
